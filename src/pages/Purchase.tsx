import React from 'react';
import Card from '../components/ui/Card';

const Purchase = () => {
    return (
        <div className="space-y-6">
            <h1 className="text-2xl font-bold text-gray-800">Einkauf erfassen</h1>
            <Card>
                <p className="text-gray-500 text-center py-8">Formular folgt...</p>
            </Card>
        </div>
    );
};

export default Purchase;
